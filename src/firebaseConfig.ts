// Firebase 콘솔 > 프로젝트 설정 > 일반 > "내 앱" 에서 발급받은 웹 앱 설정값을 그대로 붙여넣으세요.
// 이 값들은 비밀정보가 아니며(Firebase 공식 문서 기준) 공개 저장소에 커밋해도 안전합니다.
// 실제 접근 제어는 Firestore 보안 규칙(firestore.rules)과 Authentication 계정으로 이루어집니다.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
